name: upload-artifact
on:
    workflow_dispatch:
jobs:
    upload-artifact:
        runs-on: ubuntu-latest
        steps:
            - name: generatee a file
              run: echo "test report " > test.txt
              
            - name: upload artifact
              uses: actions/upload-artifact@v4
              with:
                name: test report
                path: test.txt
